require('glow').setup({
  -- Max window ratio will be 700, 800
  --width = 160,
  --height = 200,

  -- Override above width, height
  -- use ratio against nvim window
  width_ratio = 0.7,
  height_ratio = 0.8,
})
